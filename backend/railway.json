{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo START=$PORT && /app/.venv/bin/alembic upgrade head && echo MIGRATED && /app/.venv/bin/uvicorn kindred.api.main:app --host 0.0.0.0 --port ${PORT:-8000} --log-level debug 2>&1",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "healthcheckTimeout": 0
  }
}
